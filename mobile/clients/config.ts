const config = {
  API_URL: process.env.EXPO_PUBLIC_API_ENDPOINT,
  GRAPHQL_URL: process.env.EXPO_PUBLIC_GRAPHQL_API,
  DEV_GEN_GRAPHQL: process.env.EXPO_PUBLIC_DEV_GEN_GRAPHQL,
};

export default config;
